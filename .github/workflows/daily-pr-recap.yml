name: daily-report

on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch: # Allow manual trigger for testing

jobs:
  pr-recap:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Bun (required by ironcode postinstall)
        run: |
          curl -fsSL https://bun.sh/install | bash
          # Add bun to PATH for the remainder of this step and subsequent steps
          export PATH="$HOME/.bun/bin:$PATH"
          echo "PATH=$HOME/.bun/bin:$PATH" >> $GITHUB_ENV
          # Verify installation (fail the step if bun not available)
          bun --version
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install IronCode
        run: |
          npm install -g ironcode-ai@latest

      - name: Generate daily PR recap
        id: recap
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          IRONCODE_PERMISSION: |
            {
              "bash": {
                "*": "deny",
                "gh pr*": "allow",
                "gh search*": "allow",
                "git log*": "allow",
                "git show*": "allow",
                "git diff*": "allow",
                "git tag*": "allow",
                "gh release*": "allow"
              },
              "webfetch": "deny",
              "edit": "deny",
              "write": "deny"
            }
        run: |
          TODAY=$(date -u +%Y-%m-%d)

          ironcode run -m openai/gpt-4o-mini "Generate a daily activity recap for the IronCode repository.

          TODAY'S DATE: ${TODAY}

          STEP 1: Gather PR data
          Run these commands to gather PR information. ONLY include PRs created or updated TODAY (${TODAY}):

          # PRs created today
          gh pr list --repo ${{ github.repository }} --state all --search \"created:${TODAY}\" --json number,title,author,labels,createdAt,updatedAt,reviewDecision,isDraft,additions,deletions --limit 100

          # PRs with activity today (updated today)
          gh pr list --repo ${{ github.repository }} --state open --search \"updated:${TODAY}\" --json number,title,author,labels,createdAt,updatedAt,reviewDecision,isDraft,additions,deletions --limit 100

          STEP 2: Gather direct commits to dev branch
          Run these commands to get commits directly to dev branch today (not via PRs):

          # Get commits to dev branch today with file stats
          git log origin/dev --since=\"${TODAY} 00:00:00\" --until=\"${TODAY} 23:59:59\" --pretty=format:\"%H|%an|%s\" --numstat

          # For each commit, you can get more details if needed:
          # git show <commit-hash> --stat --oneline

          STEP 3: Gather releases/tags created today
          Run these commands to get releases published today:

          # Get releases published today
          gh release list --repo ${{ github.repository }} --limit 50 --json tagName,name,createdAt,publishedAt,isPrerelease,isDraft

          # Filter for releases published today (${TODAY})
          # Check if publishedAt matches today's date

          STEP 4: For high-activity PRs, check comment counts
          For promising PRs, run:
          gh pr view [NUMBER] --repo ${{ github.repository }} --json comments --jq '[.comments[] | select(.author.login != \"copilot-pull-request-reviewer\" and .author.login != \"github-actions\")] | length'

          IMPORTANT: When counting comments/activity, EXCLUDE these bot accounts:
          - copilot-pull-request-reviewer
          - github-actions

          STEP 5: Identify what matters (ONLY from today's activity)

          **Bug Fixes From Today:**
          - PRs with 'fix' or 'bug' in title created/updated today
          - Direct commits with 'fix' or 'bug' in message
          - Small bug fixes (< 100 lines changed) that are easy to review

          **High Activity Today:**
          - PRs with significant human comments today (excluding bots listed above)
          - PRs with back-and-forth discussion today

          **Quick Wins:**
          - Small PRs (< 50 lines) that are approved or nearly approved
          - PRs that just need a final review

          **Direct Commits to Dev:**
          - Commits pushed directly to dev branch today (not via PR)
          - Show commit message and key files changed
          - Group by author if multiple commits

          **Releases Today:**
          - New releases/tags published today
          - Show version number and release name
          - Indicate if prerelease or draft

          STEP 6: Generate the recap
          Create a structured recap:

          ===TELEGRAM_START===
          **Daily Activity Recap - ${TODAY}**

          **New PRs Today**
          [PRs opened today - group by type: bug fixes, features, etc.]

          **Active PRs Today**
          [PRs with activity/updates today - significant discussion]

          **Quick Wins**
          [Small PRs ready to merge]

          **Direct Commits to Dev**
          [Commits pushed directly, not via PR - show author, message, key files]

          **Releases Today**
          [New releases/tags published today - show version and type]
          ===TELEGRAM_END===

          STEP 7: Format for Telegram
          - Use Telegram markdown (**, __, etc.)
          - BE EXTREMELY CONCISE - surface what we might miss
          - Use hyperlinked PR numbers: [#1234](https://github.com/${{ github.repository }}/pull/1234)
          - Include PR author: [#1234](url) (@author)
          - For bug fixes, add brief description of what it fixes
          - Show line count for quick wins: \"(+15/-3 lines)\"
          - HARD LIMIT: Keep under 4000 characters total
          - Skip empty sections
          - Focus on PRs that need human eyes

          OUTPUT: Output ONLY the content between ===TELEGRAM_START=== and ===TELEGRAM_END=== markers. Include the markers so I can extract it." > /tmp/pr_recap_raw.txt

          # Extract only the Telegram message between markers
          sed -n '/===TELEGRAM_START===/,/===TELEGRAM_END===/p' /tmp/pr_recap_raw.txt | grep -v '===TELEGRAM' > /tmp/pr_recap.txt

          echo "recap_file=/tmp/pr_recap.txt" >> $GITHUB_OUTPUT

      - name: Post to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
            echo "Warning: TELEGRAM_BOT_TOKEN or TELEGRAM_CHAT_ID secret not set, skipping Telegram post"
            cat /tmp/pr_recap.txt
            exit 0
          fi

          # Read the recap
          RECAP_RAW=$(cat /tmp/pr_recap.txt)
          RECAP_LENGTH=${#RECAP_RAW}

          echo "Recap length: ${RECAP_LENGTH} chars"

          # Function to post a message to Telegram
          post_to_telegram() {
            local msg="$1"
            local content=$(echo "$msg" | jq -Rs '.')
            curl -s -H "Content-Type: application/json" \
                 -X POST \
                 -d "{\"chat_id\": \"${TELEGRAM_CHAT_ID}\", \"text\": ${content}, \"parse_mode\": \"Markdown\", \"disable_web_page_preview\": true}" \
                 "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage"
            sleep 1
          }

          # If under limit, send as single message
          if [ "$RECAP_LENGTH" -le 4000 ]; then
            post_to_telegram "$RECAP_RAW"
          else
            echo "Splitting into multiple messages..."
            remaining="$RECAP_RAW"
            while [ ${#remaining} -gt 0 ]; do
              if [ ${#remaining} -le 4000 ]; then
                post_to_telegram "$remaining"
                break
              else
                chunk="${remaining:0:3900}"
                last_newline=$(echo "$chunk" | grep -bo $'\n' | tail -1 | cut -d: -f1)
                if [ -n "$last_newline" ] && [ "$last_newline" -gt 500 ]; then
                  chunk="${remaining:0:$last_newline}"
                  remaining="${remaining:$((last_newline+1))}"
                else
                  chunk="${remaining:0:3900}"
                  remaining="${remaining:3900}"
                fi
                post_to_telegram "$chunk"
              fi
            done
          fi

          echo "Posted daily PR recap to Telegram"
